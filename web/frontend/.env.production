# Production build is served by the backend on the same origin (see app.js),
# so the API base is relative. This keeps the bundle host-agnostic: it works on
# localhost, on the LAN IP, and on the public IP without a rebuild.
VITE_API_URL=/api/v1
